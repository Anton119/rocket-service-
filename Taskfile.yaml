# Локальные модули: github.com/Anton119/rocket-service-/{order,inventory,payment,shared,platform,assembly}
# При смене org/user обнови module в go.mod, импорты, prefix в format (gci) и .golangci.yml

version: '3'

vars:
  GO_VERSION: '1.26.0'
  # Модули buf.build при прямом go-get с buf.build дают 403; тянуть через публичный прокси.
  GOPROXY_MOD: 'https://proxy.golang.org,direct'
  GOLANGCI_LINT_VERSION: 'v2.11.2'
  GCI_VERSION: 'v0.14.0'
  GOFUMPT_VERSION: 'v0.9.2'
  BUF_VERSION: '1.66.0'
  PROTOC_GEN_GO_VERSION: 'v1.36.11'
  PROTOC_GEN_GO_GRPC_VERSION: 'v1.6.1'
  OGEN_VERSION: 'v1.20.1'

  BIN_DIR: '{{.ROOT_DIR}}/bin'
  GOLANGCI_LINT: '{{.BIN_DIR}}/golangci-lint'
  GCI: '{{.BIN_DIR}}/gci'
  GOFUMPT: '{{.BIN_DIR}}/gofumpt'
  BUF: '{{.BIN_DIR}}/buf'
  PROTOC_GEN_GO: '{{.BIN_DIR}}/protoc-gen-go'
  PROTOC_GEN_GO_GRPC: '{{.BIN_DIR}}/protoc-gen-go-grpc'
  OGEN: '{{.BIN_DIR}}/ogen'

  MOCKERY_VERSION: 'v3.7.0'
  MOCKERY: '{{.BIN_DIR}}/mockery'

  VEGETA_VERSION: 'v12.12.0'
  VEGETA: '{{.BIN_DIR}}/vegeta'

  GOOSE_VERSION: 'v3.27.0'
  GOOSE: '{{.BIN_DIR}}/goose'

  COVERAGE_DIR: '{{.ROOT_DIR}}/coverage'
  COVERAGE_FILE: '{{.COVERAGE_DIR}}/coverage.out'
  COVERAGE_THRESHOLD: '40'

  KIBANA_URL: 'http://localhost:5601'

  MODULES: assembly inventory order payment shared platform iam

tasks:
  formatters:install:
    desc: "Устанавливает gofumpt и gci"
    cmds:
      - echo '📦 Устанавливаем gofumpt...'
      - GOBIN={{.BIN_DIR}} go install mvdan.cc/gofumpt@{{.GOFUMPT_VERSION}}
      - echo '📦 Устанавливаем gci...'
      - GOBIN={{.BIN_DIR}} go install github.com/daixiang0/gci@{{.GCI_VERSION}}
    status:
      - test -x {{.GOFUMPT}}
      - test -x {{.GCI}}

  golangci-lint:install:
    desc: "Устанавливает golangci-lint"
    cmds:
      - echo '📦 Устанавливаем golangci-lint...'
      - GOBIN={{.BIN_DIR}} go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@{{.GOLANGCI_LINT_VERSION}}
    status:
      - test -x {{.GOLANGCI_LINT}}

  buf:install:
    desc: "Устанавливает buf"
    cmds:
      - echo '📦 Устанавливаем buf...'
      - GOBIN={{.BIN_DIR}} go install github.com/bufbuild/buf/cmd/buf@v{{.BUF_VERSION}}
    status:
      - test -x {{.BUF}}

  proto:install-plugins:
    desc: "Устанавливает protoc-плагины"
    cmds:
      - echo '📦 Устанавливаем protoc-gen-go...'
      - GOBIN={{.BIN_DIR}} go install google.golang.org/protobuf/cmd/protoc-gen-go@{{.PROTOC_GEN_GO_VERSION}}
      - echo '📦 Устанавливаем protoc-gen-go-grpc...'
      - GOBIN={{.BIN_DIR}} go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@{{.PROTOC_GEN_GO_GRPC_VERSION}}
    status:
      - test -x {{.PROTOC_GEN_GO}}
      - test -x {{.PROTOC_GEN_GO_GRPC}}

  ogen:install:
    desc: "Устанавливает ogen"
    cmds:
      - echo '📦 Устанавливаем ogen...'
      - GOBIN={{.BIN_DIR}} go install github.com/ogen-go/ogen/cmd/ogen@{{.OGEN_VERSION}}
    status:
      - test -x {{.OGEN}}

  setup:
    desc: "Устанавливает все инструменты"
    deps: [formatters:install, golangci-lint:install, buf:install, proto:install-plugins, ogen:install]

  format:
    desc: "Форматирует код (gofumpt + gci)"
    deps: [formatters:install]
    cmds:
      - echo '🧹 Форматируем код через gofumpt...'
      - find . -type f -name '*.go' ! -path '*/mocks/*' ! -path '*/pkg/proto/*' ! -path '*/pkg/openapi/*' -exec {{.GOFUMPT}} -extra -w {} +
      - echo '📦 Сортируем импорты через gci...'
      # TODO: Поменяй prefix на свой модуль
      - find . -type f -name '*.go' ! -path '*/mocks/*' ! -path '*/pkg/proto/*' ! -path '*/pkg/openapi/*' -exec {{.GCI}} write -s standard -s default -s "prefix(github.com/Anton119/rocket-service-/)" {} +

  lint:
    desc: "Запускает golangci-lint"
    deps: [golangci-lint:install]
    cmds:
      - '{{.GOLANGCI_LINT}} run --allow-parallel-runners assembly/... order/... inventory/... payment/... iam/... shared/... platform/... --config=.golangci.yml'

  proto:lint:
    desc: "Проверяет .proto-файлы линтером"
    deps: [buf:install, proto:install-plugins]
    dir: shared/proto
    cmds:
      - echo '🔍 Проверяем .proto-файлы...'
      - '{{.BUF}} lint'

  proto:gen:
    desc: "Генерирует Go-код из .proto"
    deps: [buf:install, proto:install-plugins]
    dir: shared/proto
    cmds:
      - echo '🔨 Генерируем Go-код из .proto...'
      - '{{.BUF}} generate'

  ogen:gen:
    desc: "Генерирует Go-код из OpenAPI"
    deps: [ogen:install]
    dir: shared
    cmds:
      - echo '🔨 Генерируем Go-код из OpenAPI...'
      - '{{.OGEN}} -target ./pkg/openapi/order/v1 -package orderv1 -clean ./api/order/v1/order.openapi.yaml 2>/dev/null'

  gen:
    desc: "Генерирует весь код"
    cmds:
      - task: proto:gen
      - task: ogen:gen

  deps:update:
    desc: "Обновляет зависимости"
    cmds:
      - echo '🔄 Синхронизируем go.work...'
      - go work sync
      - echo '🔄 Обновляем зависимости в модулях...'
      - for mod in {{.MODULES}}; do (cd "$mod" && GOPROXY={{.GOPROXY_MOD}} go mod tidy -compat={{.GO_VERSION}}); done

  mod:tidy:
    desc: "go mod tidy во всех модулях (обход 403 buf.build через proxy.golang.org)"
    cmds:
      - for mod in {{.MODULES}}; do (cd "$mod" && GOPROXY={{.GOPROXY_MOD}} go mod tidy -compat={{.GO_VERSION}}); done

  test:api:
    desc: "Запускает API-тесты (требует Docker: task up-all)"
    deps: [migrate:all:up]
    cmds:
      - (cd order && go test -v -count=1 ./tests/)
      - (cd iam && go test -v -count=1 ./tests/)

  test:e2e:
    desc: "Сквозные e2e-тесты order (Kafka/Redpanda)"
    cmds:
      - (cd order && go test -v -count=1 -tags=e2e ./tests/e2e/...)

  mockery:install:
    desc: "Устанавливает mockery"
    cmds:
      - echo '📦 Устанавливаем mockery...'
      - GOBIN={{.BIN_DIR}} go install github.com/vektra/mockery/v3@{{.MOCKERY_VERSION}}
    status:
      - test -x {{.MOCKERY}}

  mocks:gen:
    desc: "Генерирует моки интерфейсов"
    deps: [mockery:install]
    cmds:
      - echo '🧪 Генерация моков...'
      - '{{.MOCKERY}}'

  test:
    desc: "Запускает unit-тесты с race-детектором (без order/tests и iam/tests integration)"
    cmds:
      - for mod in assembly inventory payment; do echo "=== $mod ===" && (cd "$mod" && go test -v -race -count=1 ./...); done
      - echo "=== iam ===" && (cd iam && go test -v -race -count=1 $(go list ./... | grep -v '/tests$'))
      - echo "=== order ===" && (cd order && go test -v -race -count=1 $(go list ./... | grep -v '/tests$' | grep -v '/tests/e2e'))

  coverage:clean:
    desc: "Очищает директорию с отчётами покрытия"
    internal: true
    cmds:
      - rm -rf {{.COVERAGE_DIR}}
      - mkdir -p {{.COVERAGE_DIR}}

  test:coverage:
    desc: "Тесты с покрытием бизнес-логики"
    deps: [coverage:clean]
    vars:
      TARGET_PACKAGES:
        sh: |
          for mod in assembly order inventory payment; do
            (cd "$mod" && go list ./... | grep -v "/mocks" | grep -E '/(api|service|consumer|producer)/')
          done | tr '\n' ','
    preconditions:
      - sh: test -n "{{.TARGET_PACKAGES}}"
        msg: "Не найдено пакетов для тестирования"
    cmds:
      - echo "📦 Целевые пакеты:{{.TARGET_PACKAGES}}"
      - |
        for mod in assembly order inventory payment; do
          echo "=== $mod ==="
          (cd "$mod" && go test -v -race -covermode=atomic -coverprofile={{.COVERAGE_DIR}}/${mod}.out -coverpkg={{.TARGET_PACKAGES}} ./...)
        done
      - echo "📊 Объединяем отчёты покрытия..."
      - |
        head -1 {{.COVERAGE_DIR}}/order.out > {{.COVERAGE_FILE}}
        for f in {{.COVERAGE_DIR}}/assembly.out {{.COVERAGE_DIR}}/order.out {{.COVERAGE_DIR}}/inventory.out {{.COVERAGE_DIR}}/payment.out; do
          tail -n +2 "$f" >> {{.COVERAGE_FILE}}
        done
      - echo "📊 Покрытие по функциям:"
      - go tool cover -func={{.COVERAGE_FILE}}
      - task: coverage:threshold

  coverage:threshold:
    desc: "Проверяет соответствие покрытия порогу"
    internal: true
    vars:
      COVERAGE:
        sh: go tool cover -func={{.COVERAGE_FILE}} | tail -n1 | awk '{print $NF}' | tr -d '%'
    preconditions:
      - sh: test -f {{.COVERAGE_FILE}}
        msg: "Файл покрытия не найден. Запустите task test:coverage"
    cmds:
      - echo "🧾 Общее покрытие:{{.COVERAGE}}% | Порог:{{.COVERAGE_THRESHOLD}}%"
      - cmd: test $(echo "{{.COVERAGE}} >= {{.COVERAGE_THRESHOLD}}" | bc) -eq 1
      - echo "✅ Покрытие соответствует порогу"

  coverage:html:
    desc: "Генерирует HTML-отчёт покрытия"
    deps: [test:coverage]
    vars:
      HTML_FILE: '{{.COVERAGE_DIR}}/coverage.html'
    cmds:
      - go tool cover -html={{.COVERAGE_FILE}} -o {{.HTML_FILE}}
      - echo "🌐 Отчёт сохранён в {{.HTML_FILE}}"
      - cmd: open {{.HTML_FILE}}
        platforms: [darwin]
      - cmd: xdg-open {{.HTML_FILE}}
        platforms: [linux]
      - cmd: start {{.HTML_FILE}}
        platforms: [windows]

  coverage:check:
    desc: "Проверяет покрытие без запуска тестов"
    cmds:
      - task: coverage:threshold

  goose:install:
    desc: "Устанавливает goose (миграции БД)"
    cmds:
      - echo '📦 Устанавливаем goose...'
      - GOBIN={{.BIN_DIR}} go install github.com/pressly/goose/v3/cmd/goose@{{.GOOSE_VERSION}}
    status:
      - test -x {{.GOOSE}}

  up-core:
    desc: "Создать общую сеть и поднять core контейнеры (Kafka + Observability)"
    cmds:
      - echo '[task] 🌐 Создаём общую сеть microservices-net'
      - docker network rm microservices-net 2>/dev/null || true
      - docker network create microservices-net 2>/dev/null || true
      - echo '[task] 🚀 Поднимаем core контейнеры (Kafka)'
      - docker compose -f deploy/compose/core/docker-compose.yml --env-file core.env up --build --detach
      - echo '[task] 📊 Поднимаем observability (OTEL Collector, ES, Kibana, Prometheus, Grafana, Jaeger)'
      - docker compose -f deploy/compose/observability/docker-compose.yml --env-file core.env up --build --detach

  down-core:
    desc: "Остановить core контейнеры (Kafka + Observability) и удалить сеть"
    cmds:
      - echo '[task] 🛑 Останавливаем observability'
      - docker compose -f deploy/compose/observability/docker-compose.yml --env-file core.env down --volumes
      - echo '[task] 🛑 Останавливаем core контейнеры (Kafka)'
      - docker compose -f deploy/compose/core/docker-compose.yml --env-file core.env down --volumes
      - echo '[task] 🛑 Удаляем общую сеть microservices-net'
      - docker network rm microservices-net 2>/dev/null || true

  kibana:wait:
    desc: "Ожидает готовности Kibana"
    cmds:
      - echo '⏳ Ожидаем готовности Kibana...'
      - |
        for i in $(seq 1 60); do
          curl -sSf {{.KIBANA_URL}}/api/status >/dev/null 2>&1 && break
          sleep 2
        done

  kibana:create-view:
    desc: "Создает Data View в Kibana и устанавливает его как default"
    cmds:
      - echo '📝 Создаем Data View otel-logs*...'
      - |
        RESPONSE=$(curl -sS -X POST {{.KIBANA_URL}}/api/data_views/data_view \
          -H "kbn-xsrf: true" \
          -H "Content-Type: application/json" \
          --data '{"data_view":{"title":"otel-logs*","name":"OTel Logs","timeFieldName":"@timestamp","allowNoIndex":true},"override":true}')
        DATA_VIEW_ID=$(echo "$RESPONSE" | jq -r ".data_view.id // empty")
        echo "Data View ID: $DATA_VIEW_ID"
        if [ -n "$DATA_VIEW_ID" ]; then
          curl -sS -X POST {{.KIBANA_URL}}/api/data_views/default \
            -H "kbn-xsrf: true" \
            -H "Content-Type: application/json" \
            -d "{\"data_view_id\": \"$DATA_VIEW_ID\", \"force\": true}"
          echo '✅ Data View создан и установлен как default'
        else
          echo '❌ Не удалось создать Data View'
          exit 1
        fi

  kibana:init:
    desc: "Ожидает Kibana и создает Data View для просмотра логов"
    cmds:
      - task: kibana:wait
      - task: kibana:create-view

  up-order:
    desc: Поднять Order сервис и все его зависимости
    dir: deploy/compose/order
    cmds:
      - echo "[task] 📦 Поднимаем Order с зависимостями"
      - docker compose --env-file ../../../order.env up --build --detach

  down-order:
    desc: Остановить и удалить Order сервис и все его зависимости
    dir: deploy/compose/order
    cmds:
      - echo "[task] 🛑 Останавливаем Order с зависимостями"
      - docker compose --env-file ../../../order.env down --volumes

  up-inventory:
    desc: Поднять Inventory сервис и все его зависимости
    dir: deploy/compose/inventory
    cmds:
      - echo "[task] 📦 Поднимаем Inventory с зависимостями"
      - docker compose --env-file ../../../inventory.env up --build --detach

  down-inventory:
    desc: Остановить и удалить Inventory сервис и все его зависимости
    dir: deploy/compose/inventory
    cmds:
      - echo "[task] 🛑 Останавливаем Inventory с зависимостями"
      - docker compose --env-file ../../../inventory.env down --volumes

  up-iam:
    desc: "Поднять инфраструктуру IAM сервиса (PostgreSQL + Redis)"
    cmds:
      - echo '[task] 🔐 Поднимаем IAM инфраструктуру (PostgreSQL + Redis)'
      - docker compose -f deploy/compose/iam/docker-compose.yml --env-file iam.env up --build --detach

  down-iam:
    desc: "Остановить инфраструктуру IAM сервиса"
    cmds:
      - echo '[task] 🛑 Останавливаем IAM инфраструктуру'
      - docker compose -f deploy/compose/iam/docker-compose.yml --env-file iam.env down --volumes

  vegeta:install:
    desc: "Устанавливает vegeta (HTTP нагрузочное тестирование)"
    cmds:
      - GOBIN={{.BIN_DIR}} go install github.com/tsenart/vegeta/v12@{{.VEGETA_VERSION}}
    status:
      - test -x {{.VEGETA}}

  up-all:
    desc: "Поднять инфраструктуру + сервисы (3 инстанса OrderService)"
    cmds:
      - task: up-core
      - task: up-iam
      - task: up-inventory
      - task: up-order
      - >-
        docker compose
        --env-file core.env
        -f deploy/compose/services/docker-compose.yml
        up -d --build --scale order-service=3

  down-all:
    desc: "Остановить всё"
    cmds:
      - >-
        docker compose
        --env-file core.env
        -f deploy/compose/services/docker-compose.yml
        down
      - task: down-order
      - task: down-inventory
      - task: down-iam
      - task: down-core

  load:http:
    desc: "Нагрузочный тест HTTP: POST с нарастающей нагрузкой (50 → 500 RPS за 2.5 мин)"
    deps: [vegeta:install]
    cmds:
      - |
        VEGETA="{{.VEGETA}}"
        RESULTS_DIR=$(mktemp -d)
        BODY_FILE="$RESULTS_DIR/body.json"
        echo '{"hull_uuid":"test-hull","engine_uuid":"test-engine"}' > "$BODY_FILE"
        trap 'rm -rf "$RESULTS_DIR"' EXIT

        for rate in 50 100 150 200 250 300 350 400 450 500; do
          echo "⏱️  ступень: ${rate} RPS (15 сек)"
          echo "POST http://localhost:8080/api/v1/orders" | \
            $VEGETA attack -rate="${rate}/s" -duration=15s \
              -body="$BODY_FILE" -header="Content-Type: application/json" | \
            $VEGETA encode --to json >> "$RESULTS_DIR/results.json"
        done

        $VEGETA report -type=json < "$RESULTS_DIR/results.json"

  migrate:all:up:
    desc: "Применяет миграции order, inventory и iam"
    cmds:
      - task migrate:inventory:up
      - task migrate:order:up
      - task migrate:iam:up

  migrate:order:up:
    desc: "Применяет все миграции Order-сервиса"
    deps: [goose:install]
    cmds:
      - |
        set -a
        . ./order.env
        set +a
        {{.GOOSE}} -dir "$MIGRATIONS_DIR" postgres "$DB_URI" up

  migrate:order:down:
    desc: "Откатывает последнюю миграцию Order-сервиса"
    deps: [goose:install]
    cmds:
      - |
        set -a
        . ./order.env
        set +a
        {{.GOOSE}} -dir "$MIGRATIONS_DIR" postgres "$DB_URI" down

  migrate:order:status:
    desc: "Показывает статус миграций Order-сервиса"
    deps: [goose:install]
    cmds:
      - |
        set -a
        . ./order.env
        set +a
        {{.GOOSE}} -dir "$MIGRATIONS_DIR" postgres "$DB_URI" status

  migrate:inventory:up:
    desc: "Применяет все миграции Inventory-сервиса"
    deps: [goose:install]
    cmds:
      - |
        set -a
        . ./inventory.env
        set +a
        {{.GOOSE}} -dir "$MIGRATIONS_DIR" postgres "$DB_URI" up

  migrate:inventory:down:
    desc: "Откатывает последнюю миграцию Inventory-сервиса"
    deps: [goose:install]
    cmds:
      - |
        set -a
        . ./inventory.env
        set +a
        {{.GOOSE}} -dir "$MIGRATIONS_DIR" postgres "$DB_URI" down

  migrate:inventory:status:
    desc: "Показывает статус миграций Inventory-сервиса"
    deps: [goose:install]
    cmds:
      - |
        set -a
        . ./inventory.env
        set +a
        {{.GOOSE}} -dir "$MIGRATIONS_DIR" postgres "$DB_URI" status

  migrate:iam:up:
    desc: "Применяет все миграции IAM сервиса"
    deps: [goose:install]
    cmds:
      - |
        set -a
        . ./iam.env
        set +a
        {{.GOOSE}} -dir "$MIGRATIONS_DIR" postgres "$DB_URI" up

  migrate:iam:down:
    desc: "Откатывает последнюю миграцию IAM сервиса"
    deps: [goose:install]
    cmds:
      - |
        set -a
        . ./iam.env
        set +a
        {{.GOOSE}} -dir "$MIGRATIONS_DIR" postgres "$DB_URI" down

  migrate:iam:status:
    desc: "Показывает статус миграций IAM сервиса"
    deps: [goose:install]
    cmds:
      - |
        set -a
        . ./iam.env
        set +a
        {{.GOOSE}} -dir "$MIGRATIONS_DIR" postgres "$DB_URI" status
