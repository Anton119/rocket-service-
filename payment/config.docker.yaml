# Конфигурация PaymentService для Docker.
# Поля соответствуют payment/internal/config.
logger:
  level: "info"

otel:
  endpoint: "otel-collector:4317"
  service_name: "payment-service"

grpc:
  host: "0.0.0.0"
  port: "50052"
