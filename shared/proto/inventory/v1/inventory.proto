syntax = "proto3";

package inventory.v1;

option go_package = "github.com/Anton119/rocket-service-/shared/pkg/proto/inventory/v1;inventoryv1";

import "google/protobuf/timestamp.proto";

// InventoryService управляет каталогом деталей для космических кораблей
service InventoryService {
  // GetPart возвращает деталь по UUID
  rpc GetPart(GetPartRequest) returns (GetPartResponse);

  // ListParts — список деталей с фильтром
  rpc ListParts(ListPartsRequest) returns (ListPartsResponse);
}

// Тип детали
enum PartType {
  // не задан
  PART_TYPE_UNSPECIFIED = 0;
  // корпус
  PART_TYPE_HULL = 1;
  // двигатель
  PART_TYPE_ENGINE = 2;
  // щит
  PART_TYPE_SHIELD = 3;
  // оружие
  PART_TYPE_WEAPON = 4;
}

// Part представляет деталь космического корабля
message Part {
  // Уникальный идентификатор детали
  string uuid = 1;

  // Название детали
  string name = 2;

  // Описание детали
  string description = 3;

  // Цена в копейках (5000₽ = 500000)
  int64 price = 4;

  // Тип детали (enum)
  PartType part_type = 5;

  // Количество на складе
  int64 stock_quantity = 6;

  // Дата создания
  google.protobuf.Timestamp created_at = 7;
}

// Запрос GetPart
message GetPartRequest {
  // UUID детали
  string uuid = 1;
}

// Ответ GetPart
message GetPartResponse {
  // Деталь
  Part part = 1;
}

// Запрос ListParts
message ListPartsRequest {
  // Фильтр по типу
  PartType part_type = 1;
  // Список UUID
  repeated string uuids = 2;
}

// Ответ ListParts
message ListPartsResponse {
  // Детали
  repeated Part parts = 1;
}